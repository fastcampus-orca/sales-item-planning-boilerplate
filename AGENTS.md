# 판매 아이템 기획 프로젝트 지침

스마트스토어 판매 아이템을 조사하고 후보 비교표와 기획서로 구체화하는 프로젝트다.

## 작업 시작

- 아래 참고자료의 README에서 폴더 구조와 문서별 저장 위치를 읽고, 요청받은 단계의 입력·기준·양식을 확인한다.
- `AGENTS.md`가 공통 지침 원본이며 `CLAUDE.md`는 이를 가리키는 상대 심볼릭 링크로 유지한다.

## 문서 작성

- `samples/`는 다른 소재의 형식 예시다. 내용·수치·출처를 실제 조사의 근거로 옮기지 않는다.
- 문서는 한국어로 작성하고 Markdown 굵게 표시를 사용하지 않는다. 확인한 사실과 추정·미확정 조건을 구분하고, 숫자의 출처와 계산 근거를 남긴다.

## 사업 조건 확정

인터뷰는 시장조사를 시작할 조건을 정하는 단계다. 상품별 조사로 알아낼 사실과 사용자가 결정할 조건을 구분한다.

- 대상 고객과 그 고객의 구매 목적·해결할 문제를 먼저 질문한다. 사용자가 모르겠다면 사업 조건에 맞는 선택지를 추천하고 선택을 받는다.
- 대상 고객·구매 목적, 판매 방식·소싱 경로, 예산·운영 시간, 목표·검증 기간, 제외 조건을 확인한 뒤 사업 조건을 정리한다. 이미 답한 내용은 다시 묻지 않는다.
- 사용자가 확정한 조건과 조사로 확인할 미확정 사실을 구분한다. 대상 고객·구매 목적이 미정이면 인터뷰 완료로 처리하지 않는다. 조사 요청을 받았더라도 먼저 누락된 결정만 질문하고 답을 기다린다.

## 시장조사 실행

웹에서 확인할 사실은 에이전트가 `agent-browser`로 직접 조사한다. 로그인이나 화면 조작이 필요할 것이라는 예상만으로 사용자에게 넘기지 않는다.

확정한 시장조사 기준을 실행 입력으로 옮기고 [시장조사 실행 검사](docs/research-check.md)를 따른다. 상세 수요·경쟁 조사는 소싱·수익성 선별을 통과한 상품에만 수행한다.

### 조사 착수와 전환

- 기준을 만들 때 완료 후보 수와 별개로 전체 조사 시간·선별 상품 수·공급사 수·공급사별 탈락 한도를 제안하고 확정받는다. 결과표 분량을 전체 탐색 한도로 대신하지 않는다.
- 공개 단가와 요청한 판매 방식의 거래 조건이 함께 확인되는 공급 경로부터 선별한다.
- 상품의 실제 소매가에서 목표 이익과 모든 적용 비용을 빼 허용 매입원가를 구한다. 확인한 공급가·배송비가 한도를 넘으면 상세 조사를 시작하지 않는다.
- 상품마다 누적 선별 기록을 갱신하고 실행 검사를 돌린다. `READY_FOR_DETAIL`만 상세 조사하고, 공급사 전환·한도 소진 판정을 따르면 같은 실패 경로를 자동 반복하지 않는다.
- 조사 한도를 소진하면 유효 후보 수·원인·재개에 필요한 변경을 짧게 보고한다. 사용자에게 공개 데이터 수집을 떠넘기거나 임의로 사업 조건을 바꾸지 않는다.

### 가격·근거 수집

- 사용 가능한 `agent-browser` 지침을 읽고 조사 출처를 직접 연다. 공개 페이지부터 확인하며, 도구가 실행되지 않으면 설치·연결 상태와 오류를 확인해 필요한 조치만 안내한다.
- 공급가·배송비·거래 조건, 수요 지표, 판매가·리뷰 수는 직접 수집한다. 수수료·정산 등 플랫폼 정책은 공식 안내를 우선하고, 도매 공급가를 소비자 판매가로 대체하지 않는다.
- 판매가는 공급 상품과 동일한 제품의 소매 판매 페이지에서 확인한다. 모델·규격·옵션·구성 수량을 맞추고 배송비·할인 적용 조건을 구분해, 공급 상품과 소매 상품의 이름·URL·근거 ID를 한 쌍으로 연결한다.
- 도매 사이트에 권장 소비자가가 없거나 한 소매몰에 접근할 수 없으면 남은 조사 한도 안에서 다른 공개 소매몰에서 같은 상품을 찾는다. 시장가 조사를 사용자에게 가격을 정하는 질문으로 넘기지 않는다.
- 동일 상품을 찾지 못하면 유사 상품의 차이를 밝히고 참고 가격으로만 구분한다. 서로 다른 상품의 검색 결과 중앙값을 개별 상품의 공급가·판매가로 연결하거나, 상품명·URL 없이 수집한 가격만으로 마진과 통과 여부를 확정하지 않는다.
- 접근이 막히면 시도한 URL·차단 내용을 `artifacts/sources/`에 남기고 다른 공개 출처를 찾는다. 상대 지수와 절대 검색량처럼 다른 지표를 같은 값으로 쓰지 않으며, 대체 지표가 필요하면 기준 변경을 제안한다.
- 로그인·본인 인증은 사용자에게 해당 조작만 요청하고, 완료되면 수집을 이어간다. 계정별 자격·비공개 사내 규정처럼 사용자만 확인할 수 있는 내용과 공개 조사 항목을 분리한다. 비밀번호를 요청하지 않는다.
- 수치에는 근거 ID를 붙이고 원문 URL·확인한 값·적용 조건을 기록한다. 계산은 입력값의 근거 ID와 산식을 연결하고, 추정은 설정 이유를 밝힌다.

### 마진율 계산과 조사 완료

결과표에는 확정한 시장조사 기준을 충족하고 필수 가격·비용을 확보해 예상 마진율을 계산한 상품만 싣는다.

- 동일 상품·동일 옵션·동일 수량의 판매가와 공급가, 배송비, 적용 수수료를 원문 근거와 함께 확보한다. 무료 배송이나 비용 미발생도 근거를 확인하며, 값을 찾지 못했다는 이유로 0원을 넣지 않는다.
- 필수 가격·비용을 확보하지 못하면 다른 공개 출처를 확인하고, 그래도 확보되지 않으면 사업 조건에 맞는 다른 상품으로 교체해 조사한다. 미정·미확인·보류 상품으로 조사 수량을 채우지 않는다.
- 수집 실패와 유사 상품 참고 가격은 `artifacts/sources/`에만 남긴다. 필수 가격·비용이나 예상 마진율이 비어 있는 상품은 시장조사 결과표·후보 비교표·기획서에 넣지 않는다.
- 광고 효율·반품률 등 계획 가정은 시장조사 기준에서 사용자가 확정한 값만 적용하고 근거와 산식을 연결한다. 미확인 판매가·공급가·배송비·수수료를 임의의 추정이나 유사 상품 가격으로 메우지 않는다.
- 실행 검사에서 `COMPLETE`이고 확정한 완료 조건을 충족한 경우에만 시장 조사 노트를 완성한다. 조건 미달 상품으로 수량을 채우지 않는다. 입력·근거가 바뀌면 검사를 다시 실행한다.

## 결과 검토

- 완료 보고만으로 통과시키지 않는다. 실제 문서를 확정한 양식·품질 기준과 대조하고 항목별 통과·실패 및 이유를 기록한다. 세 문서 모두 양식의 필수 섹션·표의 열을 승인 없이 삭제·대체하지 않고 채웠는지 검사한다.
- 결과표에 확정한 시장조사 기준을 충족하지 못한 상품, 필수 가격·비용의 근거가 없는 상품, 예상 마진율을 계산하지 않은 상품이 있으면 반려한다. 해당 상품을 교체해 조사하고 관련 문서를 갱신한다.
- 실패 항목은 문제 위치와 수정 내용을 기록하고 수정 후 재검사한다. 정한 기준을 낮춰 통과시키지 않는다.
- 사업 조건이 바뀌면 영향받는 문서와 검사하지 않을 문서의 이유를 기록하고, 수정·재검사 후 최종 문서를 갱신한다.

## 산출물과 저장 규칙

- 문서별 파일명과 경로는 README의 문서 저장 표를 따른다. 사업 조건·작업 기준은 `artifacts/rules/`, 작성 중 문서는 `artifacts/drafts/`, 품질 기준·검사 기록은 `artifacts/quality-gate/`에 둔다.
- 초안을 갱신하고 검사를 통과한 문서만 `artifacts/final/`에 복사한다. 이전 내용은 Git 이력으로 관리한다.
- 조사 원본은 `artifacts/sources/`에 저장한다. 양식과 샘플을 결과물로 덮어쓰지 않는다.
- 웹페이지 공유를 요청받으면 `artifacts/final/share/`만 배포 대상으로 삼고, 원문 숫자·출처·추정 표기를 유지한다. 실행하지 않은 검사·배포를 완료로 기록하지 않는다.

## 판단이 필요한 상황

- 자료가 부족하거나 서로 충돌하면 해당 원문과 영향을 제시한다. 사용자 결정이 필요한 조건을 임의로 확정하지 않는다.

## 참고자료

작업에 필요한 문서는 아래 링크에서 확인한다. 사업 조건·기준·초안·검사 기록은 현재 단계에서 생성됐는지 확인한다.

- 프로젝트 구조·저장 규칙: [README.md](README.md)
- 시장 조사 노트 양식: [market-research-template.md](templates/market-research-template.md)
- 아이템 후보 비교표 양식: [item-candidates-template.md](templates/item-candidates-template.md)
- 판매 아이템 기획서 양식: [proposal-template.md](templates/proposal-template.md)
- 형식 참고 샘플: [samples/](samples/)
- 선별·계산·탐색 종료: [시장조사 실행 검사](docs/research-check.md)
- 사업 조건·작업 기준: [artifacts/rules/](artifacts/rules/) — `business-brief.md`, `research-criteria.md`, `selection-criteria.md` 생성 후 사용
- 품질 기준·검사 기록: [artifacts/quality-gate/](artifacts/quality-gate/) — `criteria.md`, `quality-review.md` 생성 후 사용
